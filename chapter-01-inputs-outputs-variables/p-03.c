#include<stdio.h>

int main()
{
    /* Celcius to farenheit formulae
    F = (9/5)C + 32 */
    float celcius = -40;
    printf("The farenheit degree of the temperature is - %f", (celcius * 9 / 5) + 32);
    return(0);
}