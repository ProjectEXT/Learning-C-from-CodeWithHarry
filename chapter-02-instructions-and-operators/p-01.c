// i) is INVALID (Undeclared b)
// ii) is VALID (BITWISE OPERATORS)
// iii) is INVALID (character type cannot contain spaces)

#include<stdio.h>

int main(){
    int a, b; b = a;
    int v = 3 ^ 3;
    char dt = "21 dec 2022";
    return(0);
}